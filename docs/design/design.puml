@startuml
package presentation{
    +class Main{
        +{static} main(String[])
    }
    Main --> GameSetup
    Main --> GameView

    +class GameSetup{
    }
    GameSetup --> GameController
    +class GameController{

        +while loop
    }
    +class GameView{
        TBD
    }
}
presentation.GameController --> domain.TurnController

package domain{
    +class TurnController{
        +list of players
        +takeTurn() - Go back to while loop in GameController
    }
    TurnController --> Deck
    TurnController --> "*" Player
    TurnController --> Card


    +class Player{
        -hand: List<Card>

        +playCard(Card): void
        +endTurn(): void
    }
    Player --> "*" Card
    Player ..> Deck

    +class Deck{
        -deck: LinkedList<Card>
        -rand: SecureRandom

        +Deck()
        ~Deck(rand: SecureRandom)
        +insertCardAtIndex(card: Card, index: int): void
        ...

    }

    +class Card{

    }

    +enum CardType{

    }
    Card --> CardType

}
@enduml